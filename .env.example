# Copy to .env and fill in real values. Never commit .env.
PORT=3000
NODE_ENV=development

# Used to sign JWTs. Generate with: openssl rand -hex 32
JWT_SECRET=replace-me-with-a-long-random-string
JWT_EXPIRES_IN=7d
